'''
//The following code is commented to ensure
//it cannot be executed
//Do not uncomment, it is for reference only.

abs(number)  # Returns the absolute value of a number
bytes(string,encoding[, errors]) encodes a string into bytes, declares their encoding, and catches errors
cmath.sqrt(number) # Returns the sqrt of a complex number, assume (x)j where x=0 unless otherwise specified
float(object) #converts a string or number into a floating point number
help([object]) # Invokes the built-in help system. (If no argument is given, the interactive help system starts on the interpreter console.)
input(prompt) # prompts the user for input, prints the prompt string
math.ceil(number) # Round up to the nearest int
math.floor(number) # Round down the the nearest int
math.sqrt(number) # Returns the sqrt of a number, returns NaN if complex
pow(x,y[,z]) # Returns x to the power of y, if z is present returns x to the power of y, modulo z
print(object,...) # Print args to console, separated by commas, will concatenate strings
repr(object) # return a string representation of object that is generally a legal python expression
str(object) #anthithetical to repr, returns a nice human readable string but not necessarily a legal python expression
'''